@model mediatori.Models.Anagrafiche.Riferimento
@{
     Layout =null;
 }


               <div  class="editor-label">
                    @Html.LabelFor(m => m.tipoRiferimento)
                    @Html.DropDownListFor(m => m.tipoRiferimento.id, (IEnumerable<SelectListItem>)ViewBag.listaTipoRiferimento,"")<br />
                    @Html.ValidationMessageFor(m => m.tipoRiferimento.id)   
                    @Html.HiddenFor(m => m.id)           
               </div>
                  <div  class="editor-label">
                    @Html.LabelFor(m => m.valore)
                    @Html.TextBoxFor(m => m.valore)<br />
                    @Html.ValidationMessageFor(m => m.valore)
                   </div>
         
           
           
   

    
